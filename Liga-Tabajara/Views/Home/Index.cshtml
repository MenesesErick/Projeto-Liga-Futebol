@model IEnumerable<Time>

@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row" aria-labelledby="aspnetTitle">
        <h1 id="title">Bem-vindo à Liga Tabajara!</h1>
        <p class="lead">
            Confira a tabela com os times participantes da nossa liga.
        </p>
    </section>

    <!-- Tabela de Times -->
    <section class="row">
        <div class="col-md-12">
            <h2>Tabela de Times</h2>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Cidade</th>
                        <th>Estado</th>
                        <th>Ano Fundação</th>
                        <th>Estádio</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var time in Model)
                        {
                            <tr>
                                <td>@time.Nome</td>
                                <td>@time.Cidade</td>
                                <td>@time.Estado</td>
                                <td>@time.AnoFundacao</td>
                                <td>@time.Estadio</td>
                                <td>@(time.Status ? "Apto" : "Inapto")</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6">Nenhum time encontrado.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </section>

    <!-- Conteúdo Padrão do ASP.NET (opcional) -->
    <div class="row">
        <section class="col-md-4" aria-labelledby="gettingStartedTitle">
            <h2 id="gettingStartedTitle">Getting started</h2>
            <p>
                ASP.NET MVC dá a você uma forma poderosa e baseada em padrões para
                construir sites dinâmicos, permitindo uma separação de preocupações limpa e
                proporcionando controle total sobre o markup para um desenvolvimento ágil.
            </p>
            <p><a class="btn btn-outline-dark" href="https://go.microsoft.com/fwlink/?LinkId=301865">Learn more &raquo;</a></p>
        </section>
        <section class="col-md-4" aria-labelledby="librariesTitle">
            <h2 id="librariesTitle">Get more libraries</h2>
            <p>NuGet é uma extensão gratuita para o Visual Studio que facilita adicionar, remover e atualizar bibliotecas e ferramentas em projetos.</p>
            <p><a class="btn btn-outline-dark" href="https://go.microsoft.com/fwlink/?LinkId=301866">Learn more &raquo;</a></p>
        </section>
        <section class="col-md-4" aria-labelledby="hostingTitle">
            <h2 id="hostingTitle">Web Hosting</h2>
            <p>Você pode facilmente encontrar uma empresa de hospedagem que ofereça a mistura certa de recursos e preço para suas aplicações.</p>
            <p><a class="btn btn-outline-dark" href="https://go.microsoft.com/fwlink/?LinkId=301867">Learn more &raquo;</a></p>
        </section>
    </div>
</main>
