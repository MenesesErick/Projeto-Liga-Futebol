@model IEnumerable<Gol>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Jogador.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Partida.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Minuto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Jogador.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Partida.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Minuto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.GolId }) |
            @Html.ActionLink("Details", "Details", new { id = item.GolId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.GolId })
        </td>
    </tr>
}

</table>
